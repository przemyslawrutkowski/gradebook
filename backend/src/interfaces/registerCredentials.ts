export default interface RegisterCredentials {
    pesel: string;
    email: string;
    phoneNumber: string;
    password: string;
    passwordConfirm: string;
    firstName: string;
    lastName: string;
}